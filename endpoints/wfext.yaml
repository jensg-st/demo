direktiv_api: endpoint/v1
allow_anonymous: true
timeout: 2
plugins:
  outbound:
    - type: js-outbound
      configuration:
       script: |
          sleep(5)
    - type: js-outbound
      configuration:
        script: |
          log(input)
          input["Headers"].Add("demo3", "value3")
methods: 
  - GET
  - POST`