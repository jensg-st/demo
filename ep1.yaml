direktiv_api: endpoint/v1
allow_anonymous: false
plugins:
  auth:
    - type: github-webhook-auth
      configuration: 
        secret: helloworld
  target:
    type: target-flow
    namespace: test
    configuration:
        flow: /test.yaml
        async: true
methods:
  - POST
path: ep1