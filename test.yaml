direktiv_api: workflow/v1
states:
- id: a
  type: error
  error: badinput
  message: 'Missing or invalid value for required input.'
# - id: helloworld
#   type: noop
#   log: jq(.)
#   transform:
#     data: jq(.)
